---
title: "Data Import"
output: github_document
---

```{r, setup}
library(tidyverse)
```

## Read in some data

Read in the litters dataset.

Read in the litters dataset

```{r}
library(tidyverse)
library(janitor)
litters_df = read_csv("./data/FAS_litters.csv")
litters_df = clean_names(litters_df)
```

## Takr a look at the data

printing in the console

```{r}
litters_df
```

```{r}
tail(litters_df)
```

```{r}
skimr::skim(litters_df)
```

